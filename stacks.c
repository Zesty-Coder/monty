#include "monty.h"
/**
 * f_stack - prints the top of the list
 * @head: head of the stack
 * @counter: line number
 * Return: no return
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
